<div class="themed-rte @Class">
    @if (!string.IsNullOrWhiteSpace(Label))
    {
        <label class="fluent-label themed-rte-label">@Label</label>
    }

    <BlazoredTextEditor @ref="Editor"
                        ReadOnly="@Disabled"
                        Placeholder="@Placeholder"
                        Theme="snow"
                        EditorCssClass="themed-rte-editor"
                        ToolbarCSSClass="themed-rte-toolbar">
        <ToolbarContent>
            <span class="ql-formats">
                <select class="ql-header">
                    <option selected=""></option>
                    <option value="2"></option>
                    <option value="3"></option>
                </select>
                <button class="ql-bold"></button>
                <button class="ql-italic"></button>
                <button class="ql-underline"></button>
            </span>
            <span class="ql-formats">
                <button class="ql-list" value="ordered"></button>
                <button class="ql-list" value="bullet"></button>
                <button class="ql-blockquote"></button>
                <button class="ql-code-block"></button>
            </span>
            <span class="ql-formats">
                <button class="ql-link"></button>
                <button class="ql-clean"></button>
            </span>
        </ToolbarContent>
    </BlazoredTextEditor>

    @if (!string.IsNullOrWhiteSpace(HintText))
    {
        <p class="themed-rte-hint">@HintText</p>
    }
</div>
