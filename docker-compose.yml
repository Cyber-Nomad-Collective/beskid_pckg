services:
  pckg:
    build:
      context: .
      dockerfile: Dockerfile
    image: pckg:latest
    container_name: pckg
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8082
      HttpClient__InternalBaseAddress: http://127.0.0.1:8082
      ConnectionStrings__DefaultConnection: Data Source=/app/data/pckg.db
      Storage__ArtifactsRootPath: /app/packages 
    ports:
    - "8082:8082"
    volumes:
      - pckg_data:/app/data
      - pckg_packages:/app/packages
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8082/health/ready"]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  pckg_data:
  pckg_packages:
